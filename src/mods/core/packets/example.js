/**
 * Example packet definitions
 * 
 * Each packet file should export an array of packet names
 * These will be collected during build time to ensure consistent IDs
 * between client and server
 */

export default [
  'myPacket',
] 